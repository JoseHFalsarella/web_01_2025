function maiuscula(palavra: string): string {
    return palavra.toUpperCase()
}

const maiuscula2 = (palavra: string) => palavra.toUpperCase()

console.log(maiuscula('abacaxi'))
console.log(maiuscula2('maçã'))